namespace AIInfluence;

/// <summary>Slice 18 (optional): wiki retrieval for rare BLGM flags — stub until RAG pipeline exists.</summary>
public static class GameMasterWikiRag
{
	public static string TryRetrieve(string query)
	{
		return string.Empty;
	}
}
