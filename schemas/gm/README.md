# OpenRouter JSON Schemas (slice 27)

Place **generated** `json_schema` artifacts here (versioned with BLGM + mod). Runtime code may load these for `response_format` on plan-only OpenRouter calls.

- `gm_plan` — see `GameMasterCommandSerializer.BuildOpenRouterGmPlanResponseFormat()` as the in-code source of truth until codegen is wired.
